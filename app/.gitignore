node_modules/
.expo/
dist/
# Anchored: these are the prebuild output at the app root, not every folder
# that happens to be called ios or android — a native module keeps its own.
/ios/
/android/
*.log
.DS_Store
identity.local.json
